<?php
/**
 * Per-session overview page (layout owner view). Reads generated output from
 * sts/temp and renders it under a clean URL: session_overview.php?session=N.
 * The shared markup lives in session_index_template.php.
 */
require __DIR__ . '/session_index_template.php';
