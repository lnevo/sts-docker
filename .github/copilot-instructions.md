# STS UI Modernisation — Steering Document

Shared context for this repo now lives in `llm-wiki/INDEX.md`. Read the
index first, then load only the page whose `load_when` trigger matches the
task. Don't bulk-load everything.

For UI/CSS/JS conventions in the legacy `sts/` PHP app specifically, see
[`llm-wiki/ui-conventions.md`](../llm-wiki/ui-conventions.md).
